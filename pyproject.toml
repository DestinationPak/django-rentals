[build-system]
requires = ["setuptools>=68", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "django-rentals"
dynamic = ["version"]
description = "A Django Rest API for rental (vehicle/gear) listings, availability, and bookings."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Awais Jibran", email = "awaisdar001@gmail.com" }]
requires-python = ">=3.11"
keywords = ["django", "rentals", "rest", "api"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "Framework :: Django",
    "Framework :: Django :: 4.2",
    "Framework :: Django :: 5.2",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Internet :: WWW/HTTP :: Dynamic Content",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "Django>=4.2,<6.0",
    "djangorestframework>=3.16,<3.19",
    "django-filter>=23.2,<26.2",
    "drf-spectacular>=0.28.0",
    "swapper>=1.3.0",
]

[project.urls]
Homepage = "https://github.com/DestinationPak/django-rentals"
Changelog = "https://github.com/DestinationPak/django-rentals/blob/main/CHANGELOG.md"
Issues = "https://github.com/DestinationPak/django-rentals/issues"
Source = "https://github.com/DestinationPak/django-rentals"

[project.optional-dependencies]
dev = [
    "factory-boy>=3.3",
    "Faker",
    "pytest>=7.0",
    "pytest-django>=4.5.0",
    "pytest-cov>=4.0",
    "pylint>=2.0",
    "pylint-django>=2.5.0",
    "pylint-plugin-utils",
    "isort",
    "pre-commit",
    "build",
    "twine",
]
docs = [
    "sphinx>=7.0",
    "myst-parser>=2.0",
    "furo",
]

[tool.setuptools]
# PEP 621 metadata defaults this to true, which (combined with
# setuptools-scm's git-file-finder) sweeps every git-tracked file under a
# found package's directory tree into the wheel as "package data" -
# including django_rentals/api/tests/*, bypassing packages.find's exclude
# below entirely. This package ships no non-Python data files, so opting
# out is correct, not just a workaround.
include-package-data = false

[tool.setuptools.packages.find]
where = ["src"]
include = ["django_rentals*"]
# django_rentals.tests ships deliberately - django_rentals/tests/factories.py
# is a documented, consumer-facing fixture builder (see CLAUDE.md/README).
# django_rentals.api.tests is this package's own internal API test suite and
# isn't part of the public surface, so it's excluded from the built wheel.
exclude = ["django_rentals.api.tests", "django_rentals.api.tests.*"]

[tool.setuptools_scm]
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"

[tool.coverage.run]
source = ["django_rentals"]
omit = [
    "*/migrations/*",
    "*/tests/*",
    "*/tests.py",
]

[tool.coverage.report]
show_missing = true
